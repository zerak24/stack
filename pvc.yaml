apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gitlab-cache-pvc
  namespace: tools
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi

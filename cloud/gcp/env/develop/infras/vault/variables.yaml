inputs:
  ce:
    template:
      subnetwork_name: zone-2
      disk_size_gb: 30
      disk_type: pd-standard
      machine_type: e2-small
      name_prefix: vault
      tags:
        - vault-ce-allow


inputs:
  nat:
    - name: zone-1
      subnets:
        - name: subnet-01
          source_ip_ranges_to_nat: ["10.10.0.0/16"]
          # secondary_ip_range_names: ["subnet-01-pods", "subnet-01-services"]
      create_router: true
